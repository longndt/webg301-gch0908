{% extends 'base.html.twig' %}

{% block title %}Motorbike index{% endblock %}

{% block body %}
    <h1>Motorbike index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Brand</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for motorbike in motorbikes %}
            <tr>
                <td>{{ motorbike.id }}</td>
                <td>{{ motorbike.name }}</td>
                <td>{{ motorbike.brand }}</td>
                <td>{{ motorbike.price }}</td>
                <td>
                    <a href="{{ path('motorbike_show', {'id': motorbike.id}) }}">show</a>
                    <a href="{{ path('motorbike_edit', {'id': motorbike.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('motorbike_new') }}">Create new</a>
{% endblock %}
